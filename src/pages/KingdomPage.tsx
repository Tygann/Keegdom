import Container from '../components/Container'

export default function KingdomPage() {
  return (
    <main className="py-16">
      <Container>
        {/* Placeholder for a future routed page focused on the broader household, systems, and shared resources. */}
        <h1 className="font-display text-4xl font-semibold text-[var(--color-text)]">Kingdom</h1>
      </Container>
    </main>
  )
}
