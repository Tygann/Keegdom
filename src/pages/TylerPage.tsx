import Container from '../components/Container'

export default function TylerPage() {
  return (
    <main className="py-16">
      <Container>
        {/* Placeholder for a future routed landing page that can summarize Tyler's writing, projects, and links. */}
        <h1 className="font-display text-4xl font-semibold text-[var(--color-text)]">Tyler</h1>
      </Container>
    </main>
  )
}
